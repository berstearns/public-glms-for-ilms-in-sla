# Stage 07 — continued-pretrain a GLM on EFCAMDAT.
model:        !include ../common/model/glm-base.yaml
train:        !include ../common/train/continued-pretrain.yaml
conditioning: !include ../common/conditioning.yaml
cloze:        !include ../common/cloze.yaml

experiment:
  name: default
  output_dir: ./artifacts
  save_config_snapshot: true
  verbose: false
  seed: 42
  device: cpu
