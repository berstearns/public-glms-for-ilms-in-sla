# Shared "train" section — supervised-fine-tuning regime.
regime: sft
epochs: 3
batch_size: 8
grad_accum_steps: 32
learning_rate: 1.0e-5
warmup_ratio: 0.05
weight_decay: 0.01
max_grad_norm: 1.0
seed: 42
save_every_steps: 500
eval_every_steps: 500
resume_from: null
